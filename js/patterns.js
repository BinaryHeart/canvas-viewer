function drawBox1(canvasElement) {
  context = canvasElement.getContext("2d");
  context.fillRect(50, 50, 50, 50);

  //Här skall ni implementera och rita första bilden.
}

function drawBox2(canvasElement) {
  context = canvasElement.getContext("2d");
  context.fillRect(50, 50, 150, 150);

  //Här skall ni implementera och rita andra bilden.
}

function drawBox3(canvasElement) {
  context = canvasElement.getContext("2d");
  context.fillRect(150, 150, 50, 50);

  //Här skall ni implementera och rita tredje bilden.
}
